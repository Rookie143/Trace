# Checkpoints

This directory contains the three published COCO-80 YOLOv5 checkpoints:

```text
oga.pt
oda.pt
rma.pt
```

They are tracked with Git LFS. Run `git lfs pull` after cloning. SHA-256 hashes,
clean mAP, ASR, and TRACE results are listed in [RESULTS.md](../RESULTS.md).
